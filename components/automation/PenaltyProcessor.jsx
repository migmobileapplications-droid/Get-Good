import { useEffect } from 'react';

export default function PenaltyProcessor() {
  // This component is now just a placeholder
  // All penalty processing happens server-side via the processAllPenalties function
  // which should be called every 5 minutes by a cron job or similar scheduler
  
  return null;
}